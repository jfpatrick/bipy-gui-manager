import bipy_gui_manager
from bipy_gui_manager.main import main

#
# def test_main(monkeypatch):
#     # Stub test for the main switch
#     monkeypatch.setattr('sys.argv', ["bipy_gui_manager", "new"])
#     monkeypatch.setattr('bipy_gui_manager.new.new_project.new_project',
#                         lambda *a, **k: None)
#     #main()
